<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Scan-QR moved into the "+" FAB chooser; the toolbar now only
         carries Update-All, which is a list-wide operation that
         doesn't fit the per-card refresh button. -->
    <item
        android:id="@+id/action_update_all"
        android:title="@string/update_all"
        app:showAsAction="ifRoom" />
</menu>
